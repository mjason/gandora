# Gandora for VS Code

A thin client that spawns `gan lsp` (GEP-0015). Install the toolchain
(`uv tool install gandora-tool` + `uv add gandora-lsp` in your project),
then `npm install && npx vsce package` here and install the .vsix.
