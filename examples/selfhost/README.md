# selfhost

Gandora analyzing Gandora through gandora-core (GEP-0012): parse a
module, pattern-match its quoted terms to list function names, run
diagnostics, and compile-and-exec a snippet at runtime.

```console
uv add gandora-core gandora-std
gan run selfhost.gan   # from a project including this file
```
